@model WebShop.MVC.ViewModels.ProductViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Izmeni</h1>

<h4>Proizvod</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Code" class="control-label">Šifra</label>
                <input asp-for="Code" class="form-control" />
                <span asp-validation-for="Code" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Name" class="control-label">Naziv</label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Description" class="control-label">Opis</label>
                <input asp-for="Description" class="form-control" />
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Price" class="control-label">Cena</label>
                <input asp-for="Price" type="number" step="0.01" min="0.01" class="form-control" />
                <span asp-validation-for="Price" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Brand" class="control-label">Brend</label>
                <input asp-for="Brand" class="form-control" />
                <span asp-validation-for="Brand" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Sport" class="control-label"></label>
                <input asp-for="Sport" class="form-control" />
                <span asp-validation-for="Sport" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Size" class="control-label">Veličina</label>
                <input asp-for="Size" class="form-control" />
                <span asp-validation-for="Size" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="ImageFile">Slika</label>
                <input asp-for="ImageFile" type="file" class="form-control" accept="image/png, image/jpeg, image/jpg, image/gif" />
                <span asp-validation-for="ImageFile" class="text-danger"></span>
            </div>
            @if (!string.IsNullOrEmpty(Model.ImagePath))
            {
                <div class="mb-2">
                    <img id="preview" src="@Model.ImagePath" width="150" />
                    <div>
                        <button type="button" class="btn btn-sm btn-danger mt-2" id="clearImageBtn"> Obriši sliku </button>
                    </div>
                </div>
                
            }
            else
            {
                <div class="mb-2">
                    <img id="preview" class="img-thumbnail d-none" style="max-width:200px;" />
                    <div>
                        <button type="button" class="btn btn-sm btn-danger mt-2" id="clearImageBtn"> Obriši sliku </button>
                    </div>
                </div>
            }
            <input type="hidden" asp-for="ImagePath" />
            <input type="hidden" asp-for="DeleteImage" id="DeleteImage" value="false" />

            <div class="form-group">
                <label asp-for="CategoryId" class="control-label">Kategorija</label>
                <select asp-for="CategoryId" asp-items="Model.Categories" class="form-control">
                    <option value="">-- Izaberi kategoriju --</option>
                </select>
                <span asp-validation-for="CategoryId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Sačuvaj" class="btn btn-custom-red" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Nazad na proizvode</a>
</div>

@section Scripts {
    <script>
        const input = $('#ImageFile');
        const preview = $('#preview');
        const clearBtn = $('#clearImageBtn');
        const deleteFlag = $('#DeleteImage');

        input.on('change', function () {
           const file = this.files[0];
           if (!file) {
               clearImage();
               return;
           }

           const reader = new FileReader();
           reader.onload = function (e) {
               preview.attr('src', e.target.result);
               preview.removeClass('d-none');
               deleteFlag.val('false');
           };
           reader.readAsDataURL(file);
        });

        clearBtn.on('click', function () {
            clearImage();
            deleteFlag.val('true');
        });

        function clearImage() {
            input.val('')
            preview.attr('src', '');
            preview.addClass("d-none");
        }
    </script>

    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
